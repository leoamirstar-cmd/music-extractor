function initDotsAndBoxes() {
  const container = document.getElementById('board-container');
  container.innerHTML = '<p style="color: #a6e3a1;">منطق بازی نقطه و خط به‌زودی بارگذاری می‌شود...</p>';
}

